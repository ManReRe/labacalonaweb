# Build output
dist/
.astro/
.lighthouseci/

# Dependencies
node_modules/

# Logs
npm-debug.log*

# Environment
.env
.env.production

# Editor/OS
.DS_Store

# Full-resolution source photography (tens of MB each, arrives in
# differently-named folders each time) — optimized copies actually
# used by the site live in src/assets/images/, which stays tracked.
Pictures/*/
